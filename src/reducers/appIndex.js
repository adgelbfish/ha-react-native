'use strict';

import * as types from '../actions/actionTypes'

const initialState = {};

export default function appIndex(state = initialState, action = {}) {
    switch (state) {
        default:
            return state
    }
}